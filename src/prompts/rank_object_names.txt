In the previous step, a triplet was extracted from a clinical text.
The triplet contains two entities (subject and object) and one relation that connects them.
Using semantic similarity, we linked the object name with the top similar entity names from a UMLS-aligned knowledge graph built from previously seen texts.

You will be provided with the following:

Text: The original sentence or passage from which the triplet was extracted.
Extracted Triplet: A structured representation in the format { "subject": "...", "relation": "...", "object": "..." }.
Original Object: An object name that needs refinement.
Candidate Objects: A list of candidate object names (UMLS preferred names) retrieved by semantic similarity.

Your Task:
Select the most contextually appropriate object name from the Candidate Objects list that best matches the object from the extracted triplet and the context of the given Text.

- If an exact or semantically appropriate match is found, return the corresponding name exactly as it appears in the list.
- If the original object represents a date, number, dosage, percentage, or measurement (e.g., "5 mg", "2.4%", "since 1980s", "70,000 women"), return EXACTLY the original object string. DO NOT select from the Candidate Objects list in such cases — these belong as qualifiers, not as canonical entities.
- If no suitable match exists, return the string "None".
- Do not modify the name from the candidate list in case of match, add explanations, or provide any additional text.
